# 文档基线

本文档集定义“一进二”策略研究与当日实盘系统的当前实现、业务约束、架构、
数据契约、接口、质量门槛和后续生产验证要求。

## 文档状态

- 基线版本：`2.0`
- 基线日期：`2026-09-26`
- 当前实现：Python 多服务运行时 + Kafka + Flink + PostgreSQL + ClickHouse + Redis + MinIO
- 部署形态：本地 Compose 已实现，Kubernetes 基线已提供，目标集群生产验证待完成
- 数据源：仅使用 `mootdx`
- 系统边界：研究、筛选、监控和报告，不连接券商，不自动下单

文档中的状态标记：

- `CURRENT`：仓库当前已经具备并可运行的能力。
- `TARGET`：生产化迭代必须实现的目标能力。
- `DEFERRED`：已识别但不进入近期迭代的能力。

## 文档目录

| 文档 | 作用 |
| --- | --- |
| [需求规格](01-requirements.md) | 业务目标、功能需求、非功能需求和验收标准 |
| [系统架构](02-architecture.md) | 当前架构、目标架构、组件边界和可靠性设计 |
| [数据与存储](03-data-storage.md) | 数据模型、多模存储、幂等、保留和归档策略 |
| [接口规范](04-api.md) | HTTP API、事件接口、版本和错误规范 |
| [OpenAPI](openapi.yaml) | 目标 HTTP API 的机器可读契约 |
| [AsyncAPI](asyncapi.yaml) | Kafka Topic 和事件消息的机器可读契约 |
| [测试策略](05-testing.md) | 测试分层、覆盖矩阵、性能和发布门槛 |
| [部署与运维](06-deployment-operations.md) | 环境、拓扑、监控、备份、容灾和安全 |
| [迭代计划](07-iteration-plan.md) | 分阶段改造顺序、交付物和退出条件 |
| [策略研究](08-strategy-research.md) | 历史计划、严格可买分析、T+1 收益和盈利约束优化 |
| [多策略管理](09-multi-strategy.md) | 参数不可变策略、可修改名称、激活约束、血缘和交易日记录 |
| [Web UI 标准](10-web-ui-standard.md) | 四个页面的共享设计令牌、组件和响应式规则 |
| [mootdx 非实时数据持久化](11-mootdx-persistence.md) | 证券、板块、公司资料、历史行情和财务包的全量持久化设计 |

## 规范优先级

发生冲突时按以下顺序处理：

1. 需求规格中的硬性业务规则和验收条件。
2. OpenAPI、AsyncAPI 及数据唯一性约束。
3. 系统架构中的组件职责和可靠性语义。
4. 测试、部署和迭代文档。
5. 根目录 `README.md` 中的使用说明。

任何改变策略规则、存储归属、API 字段或事件 Schema 的修改，必须同时更新对应文档和
自动化测试。目标接口在实现前不得标记为 `CURRENT`。

## 当前实现

- 行情由 `market-collector` 通过 mootdx 长连接采集，并经 SQLite WAL 可靠发布到 Kafka。
- Flink 计算实时特征；`market-sink`、`strategy-engine`、`outbox-relay` 和
  `projection-worker` 分别负责行情、状态、事件和 Redis 投影。
- FastAPI 提供 `/api/v1`、SSE、健康检查、Prometheus 指标和四个 Web 页面。
- `market-reference-sync` 每个工作日 16:20 将证券目录、四类板块文件和行业配置先归档
  到 MinIO，再事务发布到 PostgreSQL；股票目录和板块 API 读取已发布快照。
- PostgreSQL 保存证券和板块时点版本、可重命名且参数不可变的策略目录，以及策略运行、
  计划、逐日记录和研究元数据。
- MinIO 保存原始主数据、版本化报告和研究归档，ClickHouse 保存行情与分钟线，Redis
  保存可重建实时投影。
- 报告调度在 `16:30`、`23:30` 运行，重启后检查最近 7 天遗漏时点并补跑。
- 已完成一年期 242 个交易日的双策略计划与实际行情回填，并保留独立研究候选。

## 2026-09-25 变更覆盖

| 变更 | 文档 |
| --- | --- |
| 调度启动补跑、失败重试、完成标记 | 需求、架构、测试、部署、Compose |
| 页面刷新执行完整报告事务并在执行时读取激活策略 | 需求、架构、接口、测试、多策略 |
| 参数不可变策略、父子血缘、关联数据永久删除、全局唯一激活 | 需求、架构、数据、接口、测试、多策略 |
| 策略列表关键参数、名称详情链接及独立重命名 | 需求、架构、数据、接口、测试、多策略、Web UI |
| 242 个交易日幂等历史回填 | 数据、测试、策略研究、多策略 |
| 年度严格可买、T+1、五日跟踪、等权盈亏 | 需求、架构、测试、策略研究 |
| 09:45 盈利约束候选及 25bp 成本 | 需求、策略研究、多策略 |
| 四页控件、表格、状态和响应式标准化 | 需求、架构、测试、Web UI 标准 |

## 2026-09-26 变更覆盖

| 变更 | 文档 |
| --- | --- |
| mootdx 全量非实时数据能力盘点与多存储归属 | 数据、mootdx 非实时数据持久化 |
| 证券、板块、公司资料的时点版本模型 | mootdx 非实时数据持久化 |
| 股票/指数统一标识、历史分时和财务包模型 | mootdx 非实时数据持久化 |

## 当前与生产验证差距

| 领域 | 已实现 | 仍需验证或完善 |
| --- | --- | --- |
| 行情采集 | 独立服务、mootdx、WAL、节点切换 | 目标节点长稳压测与持久卷故障演练 |
| 数据总线 | Kafka Topic、DLQ、幂等消费 | 多 Broker、副本和积压恢复演练 |
| 实时计算 | Flink SQL、Checkpoint、Python 排障后备 | 板块分钟样本完整度与生产负载验证 |
| 策略状态 | PostgreSQL Inbox/Outbox、不可逆状态 | 并发与跨可用区故障演练 |
| 多策略 | 不可变配置、血缘、关联数据永久删除、唯一激活 | 大规模策略目录容量验证 |
| 研究 | 月度回测、年度严格可买、T+1、盈利约束优化 | 新交易日样本外验证和真实成交滑点校准 |
| Web | 四页统一组件、桌面与 390x844 响应式 | 目标浏览器矩阵和无障碍专项审计 |
| 运维 | Compose、Kubernetes 清单、Prometheus | 目标集群容量、备份恢复、告警闭环 |

## 发布门槛

- 本文档集、OpenAPI、AsyncAPI、数据库迁移和实现保持一致。
- 快速测试、契约校验、真实组件集成测试和 Web 桌面/移动端验证通过。
- 目标环境完成容量、故障、备份和恢复演练。
- 激活策略、策略 revision、代码提交和报告运行可完整追溯。
- 所有输出继续明确“研究参考、非自动下单、非收益保证”。
